spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/listing
    username: postgres
    password: Pamplemousse345!
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
        hibernate:
          format_sql: true